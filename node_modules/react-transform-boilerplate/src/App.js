import styles from './App.css';

import React, { Component } from 'react';

import Button from './components/button/Button';

import Icon from 'babel!svg-react!./close-icon.svg?name=Icon';
import ImageTheRivalry from './the-rivalry.jpg';


export class App extends Component {
    render() {

        let foo = 'hello';


        return (
            <div>
                <Button>{'Submit'}</Button>
                <Button type="disabled">{'Submit'}</Button>
                <Button
                    block
                    type="inProgress"
                >
                    {'Submit'}
                </Button>

                <div className={styles.small}>{'small image'}</div>
                <div className={styles.large}>{'large image'}</div>
                <button className={styles.bold}>{'test'}</button>
                <Icon />
                <img src={ImageTheRivalry} />
            </div>
        );
    }
}